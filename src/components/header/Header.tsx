import Navbar from "./Navbar";

/**
 * Header component that renders the site-wide navigation bar.
 * 
 * This is a wrapper component in case future header logic or layout changes are needed.
 */
const Header = () => {
  return <Navbar />;
};

export default Header;
